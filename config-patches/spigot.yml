{
    "patches": [
        {
            "file": "/data/spigot.yml",
            "ops": [
                {
                    "$set": {
                        "path": "$.timeout-time",
                        "value": 31536000 # One year
                    }
                },
            ]
        }
    ]
}